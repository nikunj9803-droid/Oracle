CREATE TABLE STUDENT (
    STUDENT_ID NUMBER,
    STUDENT_NAME VARCHAR2(30),
    CITY VARCHAR2(30)
);

INSERT INTO STUDENT VALUES (1, 'Rahul', 'Amreli');
INSERT INTO STUDENT VALUES (2, 'Amit', 'Rajkot');
INSERT INTO STUDENT VALUES (3, 'Neha', 'Surat');

SELECT * FROM STUDENT;

CREATE MATERIALIZED VIEW STUDENT_SNAPSHOT
AS
SELECT *
FROM STUDENT;

SELECT * FROM STUDENT_SNAPSHOT;

UPDATE STUDENT SET CITY = 'Bhavnagar' WHERE STUDENT_ID = 1;

SELECT * FROM STUDENT;

SELECT * FROM STUDENT_SNAPSHOT;

BEGIN
    DBMS_MVIEW.REFRESH('STUDENT_SNAPSHOT');
END;
/

SELECT * FROM STUDENT_SNAPSHOT;
